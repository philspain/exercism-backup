#include <string>
#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H

using namespace std;

namespace hexadecimal {
    int convert(string hex_string);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H